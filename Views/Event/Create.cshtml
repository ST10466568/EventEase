@model VenueBooking.Models.Event

@{
    ViewData["Title"] = "Create";
}

<h2>Create Event</h2>

<hr />

<form asp-action="Create" method="post" enctype="multipart/form-data" id="createEventForm">
<div asp-validation-summary="All" class="alert alert-danger"></div>
    <input type="hidden" id="bookingsJson" name="bookingsJson" value="" />
    <div class="form-group">
        <label asp-for="EventName" text="Event Name" class="control-label"></label>
        <input asp-for="EventName" class="form-control" />
        <span asp-validation-for="EventName" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Description" class="control-label"></label>
        <input asp-for="Description" class="form-control" />
        <span asp-validation-for="Description" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label for="file">Upload Event Image</label>
        <input type="file" name="file" class="form-control" />
    </div>
    <div class="form-group">
        <label class="control-label">Bookings</label>
        <hr />
        <div id="bookingsContainer">
            <!-- Bookings will be added here -->
            <hr />
        </div>
        <button type="button" id="addBookingButton" class="btn btn-primary">Add Booking</button>
        <span asp-validation-for="Bookings" class="text-danger"></span>
    </div>
    <div class="form-group">
        <input type="submit" value="Create" class="btn btn-primary" />
    </div>
</form>
<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
        await Html.RenderPartialAsync("_Create_EditBookingsScriptsPartial");
    }
}
